import { StyleSheet } from "react-native";

const AppStyling = StyleSheet.create({
    root : {
        position : "absolute",
        backgroundColor : "#eee",
        width : "100%",
        height : "100%",
        paddingTop : 30
    }
})

export default AppStyling;